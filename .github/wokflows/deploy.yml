name: Network Automation

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Run Ansible
        run: |
          ansible-playbook -i inventory.ini playbook.yml